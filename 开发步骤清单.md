# ipcheck 开发步骤清单

## 当前进度

- 当前阶段：阶段 9 - 验收与优化
- 当前步骤：阶段 9 已完成并已推送
- 当前状态：五个统计窗口、本地 API、systemd timer、手动 collector、Basic Auth 公网保护、README 使用说明、180 天清理测试、GitHub 敏感文件检查均已验证；collector 已修复“未来时间日志会混入当前窗口”的统计问题，并收紧了默认 CORS 与 TLS 模板；阶段 9 结果已推送到 GitHub
- 下一步：如需继续提升 30d/90d 准确性，可补做 rotated logs 支持与 timeseries 聚合优化
- 最后更新：2026-05-10

## 使用规则

- 每完成一个步骤，必须把对应任务从 `[ ]` 改为 `[x]`。
- 每次中断前，必须更新“当前进度”。
- 每个阶段完成后，必须运行对应验证命令。
- 每个主要阶段完成后，提交并推送到 GitHub：`https://github.com/FormalistBlue/ipcheck.git`。
- 不要提交 `.env`、SQLite 数据库、日志、构建产物、密钥或真实访问日志。

## 状态标记

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成
- `[!]` 阻塞或需要用户确认

## 阶段 0：规划与项目初始化

目标：确认产品方向，建立项目骨架。

- [x] 克隆 GitHub 仓库到 `/home/ubuntu/personal/workSpace/ipcheck`
- [x] 编写 `产品设计文档.md`
- [x] 编写 `开发步骤清单.md`
- [x] 用户确认设计方案
- [x] 初始化 Git 忽略规则，排除 `.env`、数据库、日志和构建产物
- [x] 初始化项目 README
- [x] 提交并推送规划文档

完成标准：

- 产品设计文档和开发步骤清单已确认。
- GitHub 仓库包含初始文档。

验证命令：

```bash
git status --short
git log --oneline -5
```

## 阶段 1：项目技术骨架

目标：建立 Vue + Vite + Fastify + TypeScript 单仓项目。

- [x] 初始化 `package.json` 和 pnpm workspace
- [x] 创建 `apps/web` 前端项目
- [x] 创建 `apps/api` 后端项目
- [x] 创建 `packages/shared` 共享类型包
- [x] 配置 TypeScript
- [x] 配置 ESLint/Prettier 或最小格式检查
- [x] 配置根目录脚本：`dev`、`build`、`typecheck`
- [x] 添加基础 README 开发命令
- [x] 运行首次构建验证
- [x] 提交并推送阶段 1

完成标准：

- 前端和后端均可本地启动。
- `pnpm build` 通过。
- `pnpm typecheck` 通过。

验证命令：

```bash
pnpm install
pnpm build
pnpm typecheck
```

## 阶段 2：SQLite 数据层

目标：设计并实现数据库初始化、迁移和查询基础能力。

- [x] 添加 SQLite 依赖 `better-sqlite3`
- [x] 创建数据库目录策略，默认使用 `/var/lib/ipcheck/ipcheck.db`
- [x] 创建数据库初始化模块
- [x] 创建 `snapshots` 表
- [x] 创建 `snapshot_entry_counts` 表
- [x] 创建 `snapshot_top_ips` 表
- [x] 创建 `collection_runs` 表
- [x] 创建 180 天数据清理函数
- [x] 添加数据库 smoke test 或脚本
- [x] 提交并推送阶段 2

完成标准：

- 数据库可自动创建。
- 表结构满足产品设计文档。
- 清理函数不会删除 180 天内数据。

验证命令：

```bash
pnpm --filter api test
pnpm --filter api build
```

## 阶段 3：日志采集脚本

目标：新写采集脚本，不修改现有 Uptime Kuma Push 脚本。

- [x] 确认四个 OpenResty 日志路径
- [x] 实现日志路径配置
- [x] 实现状态码 `101` 过滤
- [x] 实现真实 IP 解析，读取日志末尾 `X-Forwarded-For` 字段
- [x] 实现入口名映射：ws/ws1/ws2/ws3
- [x] 实现统计窗口：30m、24h、7d、30d、90d
- [x] 实现 TOP IP 统计
- [x] 实现来源入口 breakdown
- [x] 实现快照写入 SQLite
- [x] 实现采集运行状态写入 `collection_runs`
- [x] 实现 180 天数据清理
- [x] 添加 dry-run 模式，输出 JSON 不写库
- [x] 使用真实日志运行 dry-run 验证
- [x] 提交并推送阶段 3

完成标准：

- 采集脚本可在不影响现有 Uptime Kuma 脚本的情况下运行。
- dry-run 输出包含总连接数、独立 IP、入口统计、TOP IP。
- 写库后 API 可查询到最新快照。

验证命令：

```bash
pnpm --filter api collect -- --dry-run
pnpm --filter api collect
```

## 阶段 4：Fastify API

目标：提供 Dashboard 所需接口。

- [x] 创建 Fastify 服务入口
- [x] 实现 `GET /api/health`
- [x] 实现 `GET /api/summary?range=...`
- [x] 实现 `GET /api/timeseries?range=...&bucket=...`
- [x] 实现 `GET /api/top-ips?range=...&limit=...`
- [x] 实现 `GET /api/snapshots/recent?limit=...`
- [x] 实现 `GET /api/entries?range=...`
- [x] 添加参数校验和错误响应
- [x] 添加 API smoke test
- [x] 配置只监听 `127.0.0.1`
- [x] 提交并推送阶段 4

完成标准：

- 所有 API 返回结构稳定。
- 错误参数返回可读错误。
- `GET /api/health` 返回正常。

验证命令：

```bash
pnpm --filter api dev
curl -sS http://127.0.0.1:8788/api/health
curl -sS 'http://127.0.0.1:8788/api/summary?range=7d'
```

## 阶段 5：Vue Dashboard 页面

目标：实现单页可视化 Dashboard。

- [x] 创建全局视觉主题和 CSS 变量
- [x] 引入 Rubik 和 JetBrains Mono 字体
- [x] 创建 API client
- [x] 创建 range 切换状态
- [x] 创建自动刷新逻辑，每 10 分钟刷新
- [x] 创建顶部 Hero 区
- [x] 创建核心指标卡片
- [x] 创建总连接趋势折线图
- [x] 创建分入口趋势折线图
- [x] 创建入口分布环形图
- [x] 创建入口对比柱状图
- [x] 创建 TOP 10 IP 排行榜
- [x] 创建时间热力图
- [x] 创建采集状态时间线
- [x] 创建最新统计详情卡片
- [x] 创建最近 20 次历史快照列表
- [x] 适配手机端布局
- [x] 添加加载、错误、空数据状态
- [x] 提交并推送阶段 5

阶段 5 完成记录（2026-05-09）：

- 新增 `apps/web/src/api.ts`，统一请求 summary、timeseries、recent snapshots 和 collection runs，并按范围选择 bucket。
- 重写 `apps/web/src/App.vue`，实现范围切换、10 分钟自动刷新、手动刷新、加载/错误/空数据状态，以及 Hero、指标卡、趋势图、入口分布、TOP IP、热力图、采集时间线、最新详情和历史快照列表。
- 重写 `apps/web/src/styles.css`，使用 Rubik + JetBrains Mono、深紫 Sentry 风格视觉变量、玻璃卡片、响应式桌面/手机布局和入场动效。
- 扩展共享 TopIpItem 类型，补齐 API 返回的 rank 字段。
- 验证通过：`pnpm --filter web build`；`pnpm typecheck`；`pnpm build`；`pnpm --filter api test`；`IPCHECK_DB_PATH=/tmp/ipcheck-dev.db pnpm --filter api dev` 后 `curl /api/health`、`curl /api/summary?range=7d`；`pnpm --filter web dev` 后 `curl -I http://127.0.0.1:5174/`。
- 注意：默认 API DB 路径 `/var/lib/ipcheck/ipcheck.db` 在普通用户下无权限，本轮本地冒烟使用 `/tmp/ipcheck-dev.db`，正式部署阶段由 systemd/目录权限处理。

完成标准：

- 页面展示所有 MVP 数据。
- 图表在桌面和手机都能正常显示。
- 数据刷新不会造成页面闪烁或明显卡顿。

验证命令：

```bash
pnpm --filter web dev
pnpm --filter web build
```

## 阶段 6：前后端集成与静态托管

目标：API 服务同时托管前端构建产物。

- [x] 配置前端构建输出
- [x] 配置 Fastify 静态文件服务
- [x] 配置前端 API base URL
- [x] 本地构建后验证页面可访问
- [x] 验证刷新任意路径不会 404
- [x] 提交并推送阶段 6

阶段 6 功能完成记录（2026-05-09）：

- 将 `apps/web/src/App.vue` 中的趋势、分入口、入口分布、入口对比、TOP IP 和热力图切换为 `vue-echarts` / ECharts 渲染，保留现有 Dashboard 布局和视觉风格。
- 更新 `apps/web/src/api.ts`，最近快照请求携带当前 `range`，避免 30m/24h/7d/30d/90d 快照混在同一个“最新详情”里。
- 更新 `apps/api/src/db/queries.ts` 和 `apps/api/src/app.ts`，`/api/snapshots/recent` 支持按 range 过滤，`/api/timeseries` 会按 `bucket=10m|1h|1d` 保留每个桶最新滚动快照。
- API 服务通过 `@fastify/static` 托管 `apps/web/dist`，`/api/*` 保持 API 行为，非 API 的前端路径回退到 `index.html`。
- 新增 API 测试覆盖 range 过滤、bucket 分桶和静态托管路径回退。
- 验证通过：`pnpm -r typecheck`、`pnpm --filter api test`、`pnpm --filter web build`、`pnpm --filter api build` 后 `PORT=8799 IPCHECK_DB_PATH=/tmp/ipcheck-stage6.db pnpm --filter api start`，`curl -I /`、`curl /api/health`、`curl /dashboard/history` 正常。
- 注意：ECharts 引入后 `pnpm --filter web build` 有 Vite chunk size 提示，当前产物 gzip 约 232KB，不影响运行；如后续继续扩展页面，可再做图表懒加载或 chunk 拆分。

完成标准：

- `http://127.0.0.1:8788` 可以打开完整 Dashboard。
- `/api/*` 和前端静态页面不冲突。

验证命令：

```bash
pnpm build
pnpm --filter api start
curl -sS -I http://127.0.0.1:8788/
curl -sS http://127.0.0.1:8788/api/health
```

## 阶段 7：systemd 部署

目标：在当前服务器上稳定运行 API 和采集任务。

- [x] 创建运行目录 `/var/lib/ipcheck`
- [x] 创建日志目录 `/var/log/ipcheck`
- [x] 创建 API systemd service
- [x] 创建 collector systemd service
- [x] 创建 collector systemd timer，每 10 分钟运行
- [x] 设置服务环境变量
- [x] 启动并验证 API 服务
- [x] 启动并验证 collector timer
- [x] 验证 SQLite 数据写入
- [x] 提交部署说明并推送阶段 7

阶段 7 完成记录（2026-05-09）：

- 新增 `deploy/systemd/ipcheck-api.service`，使用当前 nvm Node 路径启动 `apps/api/dist/server.js`，绑定 `127.0.0.1:8788`，读取 `/var/lib/ipcheck/ipcheck.db`，托管 `apps/web/dist`。
- 新增 `deploy/systemd/ipcheck-collector.service` 和 `deploy/systemd/ipcheck-collector.timer`，collector 每 10 分钟运行一次，也可手动 `systemctl start ipcheck-collector.service` 立即采集。
- 新增 `deploy/README.md`，记录安装、更新、验证、Node 升级维护和数据库隐私权限要求。
- 已创建 `/var/lib/ipcheck` 和 `/var/log/ipcheck`，权限为 `ubuntu:ubuntu 700`；数据库及 WAL/SHM 文件权限为 `600`。
- 已安装并启用 systemd units：`ipcheck-api.service` 为 active running；`ipcheck-collector.timer` 为 active waiting。
- 已手动运行 collector 验证真实日志采集，SQLite `/var/lib/ipcheck/ipcheck.db` 已写入快照和 collection run，最新运行状态为 `ok`。
- 部署安全加固：服务以 `ubuntu` 用户运行，设置 `UMask=0077`、`NoNewPrivileges`、`PrivateTmp`、`ProtectSystem`、`ProtectHome`、`PrivateDevices`、`ProtectKernel*`、`ProtectControlGroups`、`RestrictSUIDSGID`、`LockPersonality`、`CapabilityBoundingSet` 和 `RestrictAddressFamilies`。
- 验证通过：`pnpm --filter api test`、`pnpm -r typecheck`、`pnpm build`、`systemd-analyze verify deploy/systemd/ipcheck-api.service deploy/systemd/ipcheck-collector.service deploy/systemd/ipcheck-collector.timer`、`systemctl status ipcheck-api --no-pager`、`systemctl list-timers 'ipcheck-*' --no-pager`、`journalctl -u ipcheck-collector.service -n 20 --no-pager`、`curl http://127.0.0.1:8788/api/health`、SQLite 行数检查。

完成标准：

- `ipcheck-api.service` active。
- `ipcheck-collector.timer` active。
- 每 10 分钟有新快照。

验证命令：

```bash
systemctl status ipcheck-api --no-pager
systemctl list-timers 'ipcheck-*' --no-pager
journalctl -u ipcheck-collector.service -n 80 --no-pager
```

## 阶段 8：1Panel/OpenResty 反代

目标：通过 `https://ipcheck.shandawang.cc` 访问 Dashboard。

- [x] 确认 `ipcheck.shandawang.cc` Cloudflare DNS 已指向当前服务器并开启橙云
- [x] 在 1Panel/OpenResty 创建或配置反代站点
- [x] 反代到 `http://127.0.0.1:8788`
- [x] 开启 HTTPS 证书
- [x] 验证 Cloudflare 公网访问
- [x] 检查 OpenResty 配置测试通过后 reload
- [x] 记录部署配置
- [x] 提交并推送阶段 8

阶段 8 完成记录（2026-05-09）：

- 确认 `ipcheck.shandawang.cc` DNS 返回 Cloudflare 代理 IP，公网访问已走橙云；配置前公网 HTTPS 为 525，说明 Cloudflare 已到达但源站缺少匹配 TLS vhost。
- 新增 OpenResty vhost：`/opt/1panel/apps/openresty/openresty/conf/conf.d/ipcheck.shandawang.cc.conf`，显式监听 `10.0.0.3:80` 和 `10.0.0.3:443 ssl http2`，避免多 IP 环境下 wildcard listener 引发 525。
- 新增 proxy include：`/opt/1panel/apps/openresty/openresty/www/sites/ipcheck.shandawang.cc/proxy/root.conf`，反代到 `http://127.0.0.1:8788`，保留 `Host`、`X-Forwarded-*`、WebSocket upgrade、禁用代理缓冲并设置长超时。
- 已复制现有 Cloudflare Origin wildcard 证书到 `ipcheck.shandawang.cc` 站点 ssl 目录，HTTPS 源站 SNI 测试成功；HTTP 会 301 跳转 HTTPS。
- 已启用 Basic Auth，复用现有管理类站点的 auth file 模式；未认证公网请求返回 `401`，认证后 `/api/health` 返回 ipcheck API JSON，`/` 返回 Vue Dashboard HTML。
- 已将 `ipcheck.shandawang.cc` 加入 1Panel WAF `sites.json`，避免 unknownWebsite 拦截；已运行 OpenResty 配置测试并 reload。
- 已在仓库记录非敏感配置模板：`deploy/openresty/ipcheck.shandawang.cc/ipcheck.shandawang.cc.conf`、`deploy/openresty/ipcheck.shandawang.cc/root.conf` 和 `deploy/openresty/README.md`。
- 验证通过：`curl -I https://ipcheck.shandawang.cc` 返回 401 Basic Auth；`curl -i https://ipcheck.shandawang.cc/api/health` 未认证返回 401；临时探测账号验证认证后公网 `/api/health` 和 Dashboard HTML 正常，随后已移除临时账号；`curl http://127.0.0.1:8788/api/health` 正常。

完成标准：

- `https://ipcheck.shandawang.cc` 可访问 Dashboard。
- API 请求正常。
- 图表正常加载。

验证命令：

```bash
curl -sS -I https://ipcheck.shandawang.cc
curl -sS https://ipcheck.shandawang.cc/api/health
```

## 阶段 9：验收与优化

目标：确认功能完整、体验稳定、安全边界清楚。

- [x] 验证 30m/24h/7d/30d/90d 窗口切换
- [x] 验证 TOP IP 与日志抽样一致
- [x] 验证四入口统计与日志抽样一致
- [x] 验证移动端布局
- [x] 验证 10 分钟自动刷新
- [x] 验证 180 天清理逻辑
- [x] 检查 GitHub 是否无敏感文件
- [x] 更新 README 使用说明
- [x] 最终提交并推送

完成标准：

- 用户可以通过公网域名查看 Dashboard。
- 数据与真实日志一致。
- 服务重启后自动恢复。
- 文档足够未来继续维护。

验证命令：

```bash
git status --short
pnpm build
curl -sS https://ipcheck.shandawang.cc/api/health
```

阶段 9 完成记录（2026-05-10）：

- 实际验证本地 API 五个范围接口：`30m`、`24h`、`7d`、`30d`、`90d` 均返回正常数据；手动和定时 collector 运行均写入新的 snapshots 与 collection runs。
- 通过 dry-run 与 SQLite 最新快照交叉检查，确认 TOP IP 和四入口统计与当前日志抽样结果一致；其中 `30m` 仅统计最近窗口内的数据。
- 修复 collector 窗口边界问题：采集时现在会排除晚于本次快照时间的日志，避免“未来时间日志”提前混入当前窗口；新增回归测试覆盖该问题。
- 增强异常表达：当日志读取失败时，快照状态会标记为 `warning` 或 `error`，避免部分日志缺失时仍显示为完全正常。
- 收紧默认暴露面：API 默认不再开放任意跨域；仅在显式设置 `IPCHECK_CORS_ORIGIN` 时启用 CORS。OpenResty TLS 模板已收紧到 `TLSv1.2` / `TLSv1.3`。
- 将测试中的示例 IP 替换为 RFC 5737 文档保留地址，降低把真实日志样例带入仓库的风险。
- README 已补齐本地开发、API、collector、systemd、OpenResty、Phase 9 验证结果和安全注意事项。
- 仍保留一个后续优化项：当前 30d/90d 统计依赖源日志文件保留时长，尚未读取 rotated logs；这不阻塞当前阶段完成，但应作为后续性能/准确性优化候选。

## 验证记录

### 2026-05-09

- 已克隆空仓库：`https://github.com/FormalistBlue/ipcheck.git`
- 已创建产品设计文档和开发步骤清单。
- 用户已确认：公网访问前通过 1Panel/OpenResty 反代密码保护；应用本身第一版不做登录。
- 已创建 `.gitignore` 和 `README.md`。
- 已提交并推送初始规划文档：`359d3ff docs: add ipcheck product plan`。
- GitHub HTTPS 推送缺少凭据，已验证 SSH 可用并将 origin 改为 `git@github.com:FormalistBlue/ipcheck.git`。
- 阶段 1 已完成：创建 pnpm workspace、`apps/web`、`apps/api`、`packages/shared`，并添加基础 Vue 页面和 Fastify health/summary API。
- 验证通过：`pnpm install`、`pnpm build`、`pnpm typecheck`、`pnpm lint`、`curl http://127.0.0.1:8788/api/health`。
- 公网域名已从 `xc.shandawang.cc` 更新为 `ipcheck.shandawang.cc`。
- 阶段 2 按 TDD 完成：先添加失败测试，再实现 SQLite 数据层；better-sqlite3 因 pnpm 阻止构建脚本需手动运行 `pnpm --dir node_modules/.pnpm/better-sqlite3@12.9.0/node_modules/better-sqlite3 run build-release`。
- 阶段 2 验证通过：`pnpm --filter api test`、`pnpm --filter api build`、`pnpm build`、`pnpm typecheck`、`pnpm lint`、API health/summary smoke test。
- 阶段 3 按 TDD 完成：新增 `apps/api/src/collector/index.ts`、`apps/api/src/collect.ts` 和采集测试；确认四个 OpenResty 日志路径，解析 101 WebSocket 行尾部 `X-Forwarded-For` 真实 IP，支持 30m/24h/7d/30d/90d 窗口、TOP IP、入口 breakdown、dry-run 和写库。
- 阶段 4 按 TDD 完成：新增 `apps/api/src/db/queries.ts` 和 `apps/api/src/app.test.ts`，实现 `/api/summary`、`/api/timeseries`、`/api/top-ips`、`/api/snapshots/recent`、`/api/entries`、`/api/runs/recent` 与参数错误响应，API 默认监听 `127.0.0.1:8788`。
- 阶段 3/4 验证通过：`pnpm --filter api test`、`pnpm build`、`pnpm typecheck`、`pnpm --filter api collect -- --dry-run --tail-lines 5000`。
- 真实日志 dry-run 通过：读取 4 个日志、扫描 20000 行、匹配 13566 条 101 连接，输出包含总连接数、独立 IP、入口统计和 TOP IP。
- 写库/API smoke test 通过：采集写入 `/tmp/ipcheck-smoke.db` 后，`curl http://127.0.0.1:8788/api/health` 和 `curl 'http://127.0.0.1:8788/api/summary?range=7d'` 返回正常。
- 阶段 5 完成：新增 Dashboard API client，完成 Vue 单页 Dashboard、范围切换、10 分钟自动刷新、Hero、指标卡、趋势图、入口分布、TOP IP、热力图、采集时间线、最新详情、历史快照、移动端布局和加载/错误/空数据状态。
- 阶段 5 验证通过：`pnpm --filter web build`、`pnpm typecheck`、`pnpm build`、`pnpm --filter api test`、API 和 Web dev curl 冒烟。
- 阶段 6 完成并提交推送：API 已托管前端构建产物，Dashboard 图表切换为 ECharts，快照接口支持 range 过滤，timeseries 支持 bucket 分桶。
- 阶段 7 完成并部署：systemd API 服务与 collector timer 已安装启用，真实日志采集写入 `/var/lib/ipcheck/ipcheck.db`，本地 API health 正常。
- 阶段 8 完成：`ipcheck.shandawang.cc` 已通过 Cloudflare + OpenResty HTTPS + Basic Auth 反代到本机 `127.0.0.1:8788`，公网未认证返回 401，认证后 Dashboard/API 正常。

## 中断恢复说明

如果开发中断，下一位执行者应：

1. 阅读本文件顶部“当前进度”。
2. 阅读 `产品设计文档.md`。
3. 运行 `git status --short` 检查未提交修改。
4. 找到第一个未完成 `[ ]` 步骤。
5. 查看对应阶段的完成标准和验证命令。
6. 从最小未完成步骤继续。
7. 完成后更新本文件并提交推送。
